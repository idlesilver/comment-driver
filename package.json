{
  "name": "smart-separators",
  "displayName": "Smart Separators",
  "description": "A fork of Comment Divider focused on multi-cursor editing, smart reruns, and cleaner code section separators.",
  "version": "0.5.1",
  "publisher": "idlesilver",
  "license": "GPL-3.0",
  "icon": "img/logo_256.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/idlesilver/smart-separators"
  },
  "bugs": {
    "url": "https://github.com/idlesilver/smart-separators/issues"
  },
  "engines": {
    "vscode": "^1.28.0"
  },
  "categories": [
    "Other",
    "Snippets"
  ],
  "keywords": [
    "comment",
    "separator",
    "divider",
    "section",
    "multi-cursor",
    "toggle"
  ],
  "activationEvents": [
    "onCommand:smart-separators.makeSubHeader",
    "onCommand:smart-separators.makeMainHeader",
    "onCommand:smart-separators.insertSolidLine"
  ],
  "contributes": {
    "commands": [
      {
        "command": "smart-separators.makeSubHeader",
        "title": "Smart Separators: Make Subheader"
      },
      {
        "command": "smart-separators.makeMainHeader",
        "title": "Smart Separators: Make Main Header"
      },
      {
        "command": "smart-separators.insertSolidLine",
        "title": "Smart Separators: Insert Solid Line"
      }
    ],
    "keybindings": [
      {
        "command": "smart-separators.makeSubHeader",
        "key": "alt+x",
        "when": "editorTextFocus"
      },
      {
        "command": "smart-separators.makeMainHeader",
        "key": "shift+alt+x",
        "when": "editorTextFocus"
      },
      {
        "command": "smart-separators.insertSolidLine",
        "key": "alt+y",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Smart Separators",
      "properties": {
        "smart-separators.length": {
          "type": "number",
          "default": 80,
          "description": "Set line length for all dividers."
        },
        "smart-separators.shouldLengthIncludeIndent": {
          "type": "boolean",
          "default": false,
          "description": "Set whether the divider will be shrink consider indent size, or will be always fixed length."
        },
        "smart-separators.mainHeaderFiller": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1,
          "default": "-",
          "description": "Set symbol for main header line filling (only one)."
        },
        "smart-separators.mainHeaderHeight": {
          "type": "string",
          "enum": [
            "block",
            "line"
          ],
          "default": "block",
          "description": "Set main header vertical style."
        },
        "smart-separators.mainHeaderAlign": {
          "type": "string",
          "enum": [
            "center",
            "left",
            "right"
          ],
          "default": "center",
          "description": "Set main header text align."
        },
        "smart-separators.mainHeaderTransform": {
          "type": "string",
          "enum": [
            "uppercase",
            "lowercase",
            "titlecase",
            "none"
          ],
          "default": "none",
          "description": "Set main header text transform style."
        },
        "smart-separators.subheaderFiller": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1,
          "default": "-",
          "description": "Set symbol for subheader line filling (only one)."
        },
        "smart-separators.subheaderHeight": {
          "type": "string",
          "enum": [
            "block",
            "line"
          ],
          "default": "line",
          "description": "Set subheader vertical style."
        },
        "smart-separators.subheaderAlign": {
          "type": "string",
          "enum": [
            "center",
            "left",
            "right"
          ],
          "default": "center",
          "description": "Set subheader text align."
        },
        "smart-separators.subheaderTransform": {
          "type": "string",
          "enum": [
            "uppercase",
            "lowercase",
            "titlecase",
            "none"
          ],
          "default": "none",
          "description": "Set subheader text transform style."
        },
        "smart-separators.lineFiller": {
          "type": "string",
          "minLength": 1,
          "maxLength": 1,
          "default": "-",
          "description": "Set symbol for solid line filling."
        },
        "smart-separators.languagesMap": {
          "type": "object",
          "uniqueItems": true,
          "patternProperties": {
            "^[\\w\\-\\.]+$": {
              "type": "array",
              "description": "Name of the language mode given by VSCode",
              "minItems": 1,
              "maxItems": 2,
              "items": {
                "type": "string"
              }
            }
          },
          "additionalProperties": false,
          "description": "Defines what characters will be used as comment line limiters for special languages. It also overrides the default preset."
        }
      }
    }
  },
  "main": "./out/extension",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "prettier": "^2.2.1",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^4.1.2",
    "typescript-tslint-plugin": "^0.3.1",
    "vscode": "^1.1.26"
  }
}
